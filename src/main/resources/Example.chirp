#import "chirp.std";

pub class Main {

    var x = 100;

    // crude example to demonstrate how I want errors to work.
    pub fn run() {
        access("hidden folder");
    }

    // cannot return value of void function. Must end with a semi colon.
    // run(void) ;

    // boolean value variable
    var result = run(void)::Ok(accepted -> { continue; })::Err(err -> { std::coutln(err::msg(); });

}